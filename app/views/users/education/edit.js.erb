$('#DIVID_LIGHT_BOX_CONTAINER_MAIN').css("height", '');
$("#DIVID_LIGHT_BOX_CONTAINER_MAIN").css("overflow","hidden");

<% if @education.errors.any? %>
  <% @education.errors.full_messages.each do |message| %>
    displayGrowlMessages("error","Error!", "<%= message %>", "topRight");
  <% end %>
<% end %>


content = "<%= escape_javascript(render(:partial=>'/users/education/edit')) %>";
// Show PopUp Window
options = {'width': 700, 'height':400, 'content':content, 'heading':'Edit Education', 'showHeader':true }
showPopUpWindow(options);
